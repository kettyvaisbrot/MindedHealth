[pytest]
# insights_service and ai_microservice are separate FastAPI services with
# their own requirements.txt and their own test runs -- this pytest.ini is
# for the Django app only, so it must not try to collect their tests.
norecursedirs = insights_service ai_microservice .git venv env node_modules
